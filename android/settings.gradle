pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

plugins {
    // 明确告诉云端：我们使用 8.1.0 版本的安卓应用插件
    id "com.android.application" version "8.1.0" apply false
    // 明确声明 Kotlin 语言插件版本
    id "org.jetbrains.kotlin.android" version "1.8.22" apply false
    // 绑定 Flutter 官方构建工具
    id "dev.flutter.flutter-gradle-plugin" version "1.0.0" apply false
}

include ":app"
